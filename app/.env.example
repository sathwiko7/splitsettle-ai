# Optional: without this, receipt parsing and reminders fall back to
# deterministic rule-based logic so the app still runs end-to-end.
ANTHROPIC_API_KEY=your-key-here

# Optional: without these, "Create Payment Link" generates a mock link instead
# of a real Razorpay one. Get test keys from Dashboard > Settings > API Keys
# (Test Mode) — they start with rzp_test_. Never commit live keys.
RAZORPAY_KEY_ID=rzp_test_TW4kY9eKu8dCmP
RAZORPAY_KEY_SECRET=ekcw8mloMAKY8bhSkUct5fsl
